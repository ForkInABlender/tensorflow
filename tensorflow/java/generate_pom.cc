/* 
POM is of MAVEN package tree, a obscelete "package manager", aka bloatware. 

The initial "developer" failed to rectify such dependencies with their own wherewithall, so it has been done for that "dev" due to that "dev"'s ignoramous failing to.
*/
